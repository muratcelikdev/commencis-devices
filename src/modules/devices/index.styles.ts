import styled from 'styled-components';
import { Button as AntButton, ButtonProps as AntButtonProps } from 'antd';

export const AddDeviceButton = styled(AntButton)<AntButtonProps>`
    width: 500px;

    margin-bottom: 24px;
`;
